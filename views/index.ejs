<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Dashboard</title>
    <link href="/css/output.css" rel="stylesheet" />
  </head>
  <body class="min-h-screen bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-white">
    <div class="max-w-3xl mx-auto p-6">
      <header class="flex items-center justify-between mb-6">
        <h1 class="text-2xl font-bold">Dashboard</h1>
        <div>
          <% if (user) { %>
            <span class="mr-4">Hello, <strong><%= user.username %></strong></span>
            <form action="/auth/logout" method="post" class="inline">
              <button class="px-3 py-1 bg-red-500 text-white rounded">Logout</button>
            </form>
          <% } else { %>
            <a href="/auth/login" class="px-3 py-1 bg-primary-600 text-white rounded">Login</a>
            <a href="/auth/register" class="ml-2 px-3 py-1 bg-gray-200 dark:bg-gray-800 rounded">Register</a>
          <% } %>
        </div>
      </header>

      <main>
        <p class="text-lg">Welcome to your dashboard.</p>
      </main>
    </div>
  </body>
</html>